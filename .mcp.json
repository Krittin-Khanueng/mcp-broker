{
  "mcpServers": {
    "broker": {
      "command": "bun",
      "args": ["${CLAUDE_PLUGIN_ROOT}/src/index.ts"],
      "env": {
        "BROKER_DB_PATH": "${CLAUDE_PLUGIN_ROOT}/broker.db"
      }
    }
  }
}
